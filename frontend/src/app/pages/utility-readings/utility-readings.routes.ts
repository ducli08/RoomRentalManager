import { Routes } from '@angular/router';
import { UtilityReadingsComponent } from './utility-readings.component';

export const UTILITY_READINGS_ROUTES: Routes = [
  { path: '', component: UtilityReadingsComponent },
];
